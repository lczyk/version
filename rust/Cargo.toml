[package]
name = "version"
version = "0.1.0"
edition = "2024"
license = "MIT"
repository = "https://github.com/lczyk/version"
description = "Format a version string from its parts. Includes a build-script helper for git/date env vars."
